# LiveAct face anchors V1 fixtures (#399)

- `minimal-face.glb` — generated by `liveact-face-anchors-v1-check.mjs` (local only).
- `face-anchors.json` — committed example `SagaDriveFaceAnchorsV1` manifest for `FaceMesh` / triangle 0.

Production runs (m5/f5):

- `assets/species-3d/human/runs/<run-id>/face-anchors.json`
- `assets/species-3d/<species>/runs/<run-id>/face-anchors.json`

Authoring:

```bash
node scripts/liveact-face-anchor-author.mjs \
  --input .qa/fixtures/liveact-face-anchors-v1/minimal-face.glb \
  --output .qa/fixtures/liveact-face-anchors-v1/face-anchors.json \
  --node FaceMesh \
  --bootstrap-minimal
```

Validate against GLB:

```bash
node -e "import { validateFaceAnchorsManifestFile } from './scripts/lib/liveact-face-anchor-validate.mjs'; const r=await validateFaceAnchorsManifestFile({ manifestPath: '.qa/fixtures/liveact-face-anchors-v1/face-anchors.json', glbPath: '.qa/fixtures/liveact-face-anchors-v1/minimal-face.glb' }); console.log(r);"
```
