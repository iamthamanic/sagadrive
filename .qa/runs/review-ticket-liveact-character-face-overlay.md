# review-ticket: liveact-character-face-overlay (#400)

## Scope
Mesh-bound character face overlay + metrics HUD; no scene mutation; no billboard fallback.

## Risk
Low — additive debug path; portrait uses existing suppress sampling.
