# Feature: character-notes-statistics-tab

<!-- auto-generated by /implement on 2026-08-27 -->

## Intent
Move free-form character notes into the Hintergrund tab and add a Statistik tab that shows adventure participation arcs with a persisted development history per project.

## Preconditions
- User is authenticated (or local-admin for UI-only paths)
- Character editor is open
- For live Statistik arcs: character has been saved and may be linked via `project_members.character_id`

## Happy Path
- [x] Editor tabs are Spezies, Hintergrund, Parameter, Look, Inventar, Statistik (no Notizen tab)
- [x] Notizen textarea lives under Hintergrundgeschichte and still saves to `characters.notes`
- [ ] Statistik lists adventure arcs for the saved character (synced from project memberships)
- [ ] User can append a development entry (kind + title + detail) to an arc and see it after reload
- [x] Empty Statistik states explain “save first” and “no adventures yet”

## Edge Cases
- [x] Unsaved character: Statistik shows save-first guidance, no service crash
- [ ] Membership without arc: load upserts an active arc
- [ ] Arc without membership: remains visible as historical completed/left
- [x] Invalid development payload rejected with clear error

## Regression
- [x] Species / background / parameter flows still work
- [x] Character save still persists notes and sagadrive_profile

## Assumptions
- Migration number is `009` so it does not collide with upcoming world-profile `008` on the other branch
- v1 writes developments only via Statistik form (no auto-log from level-up yet)

## Screenshots
| Step | Filename |
|------|----------|
| 1 | `09-notes-in-background.png` |
| 2 | `13-statistics-tab.png` |

## Security Coverage
- Owner-scoped RLS on `character_adventure_arcs` via character ownership; INSERT also requires active `project_members` link; `character_id`/`project_id` immutable on UPDATE
- Active project members can read arcs for their project (SELECT only)
- Client writes only through authenticated owner paths

## Implementation Notes
- Removed editor tab `notes`; added `statistics` (`Spezies | Hintergrund | Parameter | Look | Inventar | Statistik`).
- `CharacterNotesSection` mounts under Hintergrundgeschichte; save still writes `characters.notes`.
- Migration `009_character_adventure_arcs.sql` + `characterAdventureArc.service` sync memberships → arcs and append developments JSONB.
- `CharacterStatisticsPanel` loads/syncs arcs for `savedCharacterId`, empty/save-first states, form „Entwicklung eintragen“.
- E2E updated (`09-notes-in-background.png`, `13-statistics-tab.png`); regression contract extended for tabs/arcs/notes section.

## Composition Gate
See `.qa/runs/composition-gate-character-notes-statistics-tab.md` (CLEAR: membership → arc upsert → development append → reload; notes hop unchanged via character save).
