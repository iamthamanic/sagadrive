# SagaDrive item-icon sources (PNG)

Intermediate rasters from Cursor image generation.

- Gitignored (`*.png` / `*.jpg`) — not required for normal app development.
- Force-add when submitting a PR that should run `.github/workflows/vectorize-item-icons.yml`.
- Production assets are SVG: `public/assets/items/{slug}.svg`.
