# Dependencies
node_modules/

# Build / env
dist/
.env
.env.local
.env.*.local

# Supabase backups (keep only LATEST in repo)
supabase/backups/*.sql
!supabase/backups/schema_and_data_LATEST.sql
*.dump

# IDE / OS
.idea/
.DS_Store
*.log
